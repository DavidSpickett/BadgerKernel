parameters:
  toolchain_url:

steps:
- script: |
    mkdir toolchain
    wget -qO- "${{parameters.toolchain_url}}" | tar xaf - -C toolchain --strip-components=1
  workingDirectory: $(Agent.BuildDirectory)
  displayName: download-arm-toolchain
