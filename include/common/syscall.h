/* Generated by scripts/gen_syscalls.py */
#ifndef COMMON_SYSCALL_H
#define COMMON_SYSCALL_H

#include <stddef.h>

typedef enum {
  /* [[[cog
  import cog
  from scripts.syscalls import syscalls
  for n, syscall_info in enumerate(syscalls):
    syscall, _, _ = syscall_info
    cog.outl("syscall_{} = {},".format(syscall, n))
  ]]] */
  syscall_add_thread = 0,
  syscall_get_thread_property = 1,
  syscall_set_thread_property = 2,
  syscall_set_kernel_config = 3,
  syscall_yield = 4,
  syscall_get_msg = 5,
  syscall_send_msg = 6,
  syscall_thread_wait = 7,
  syscall_thread_wake = 8,
  syscall_thread_cancel = 9,
  syscall_mutex = 10,
  syscall_condition_variable = 11,
  syscall_open = 12,
  syscall_read = 13,
  syscall_write = 14,
  syscall_lseek = 15,
  syscall_remove = 16,
  syscall_close = 17,
  syscall_exit = 18,
  syscall_malloc = 19,
  syscall_realloc = 20,
  syscall_free = 21,
  syscall_list_dir = 22,
  /* [[[end]]] */
  syscall_eol,
} Syscall;

#endif /* ifdef COMMON_SYSCALL_H */
