/* Empty file to keep the build system happy.
   We can't place the AArch64 vectors at 0
   because the distance to the RAM is too far
   to branch.
   (why Arm is able to make it but AArch64 isn't,
   I haven't been able to figure out)
*/
